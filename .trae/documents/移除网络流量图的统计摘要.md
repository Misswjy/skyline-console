## 目标

- 删除“网络流量（Network Traffic）”图卡右上角的 Max/Avg/P95 统计摘要。

## 变更点

- 在 `src/pages/compute/containers/Instance/Detail/Monitor/index.jsx` 的 Network Traffic 配置中移除 `extra` 属性函数，仅保留查询与渲染逻辑。

## 验证

- 打开虚拟机监控页，网络图右上角不再显示统计摘要，其他功能不受影响。
