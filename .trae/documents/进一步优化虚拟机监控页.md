## 优化方向（优先级从高到低）

### 数据与口径

- 对齐“自创建至今”与“今日/7 天/30 天”的统计口径，继续基于扩展 API 的创建时间生成动态区间别名，确保所有查询语义一致；涉及 `src/pages/compute/containers/Instance/Detail/Monitor/index.jsx:142-167, 566-586` 与指标定义 `src/resources/prometheus/metricDict.js:589`。
- 统一设备筛选的标签键，避免跨卡片参数污染：网络使用 `target_device`、磁盘使用 `device`；核验并在图卡级保持隔离，参考卡片处理键 `src/pages/compute/containers/Instance/Detail/Monitor/index.jsx:296-305` 与 `src/components/PrometheusChart/BaseCard.jsx:42-49`。

### 交互与呈现

- 增加“自创建至今总量”顶部数卡（不分出/入，汇总字节），更醒目；位置与样式复用现有顶部卡结构，参考 `src/pages/compute/containers/Instance/Detail/Monitor/index.jsx:36-128`。
- 单位切换：为网络速率图加入 B/s 与 bps 切换（`traffic` ↔ `bandwidth`），使用 `getSuitableValue` 的类型映射实现，参考 `src/resources/prometheus/monitoring.js:32-61` 与图卡 `scale.y.formatter` 使用点 `src/pages/compute/containers/Instance/Detail/Monitor/index.jsx:300-307`。
- 辅助统计：在图卡 `extra` 区域显示当前区间的最大值、平均值、P95；参考扩展位点 `src/components/PrometheusChart/BaseCard.jsx:98-121`。

### 查询与性能

- 查询窗口自适配：在网络、磁盘等速率类查询中统一以 `window = clamp(2*step, 3m, 30m)` 的策略替换固定窗口，已在网络实现，推广到磁盘；参考替换钩子 `convertUrl` 的上下文传参支持 `src/components/PrometheusChart/utils/index.js:23-26`。
- 取消控制：刷新/切换频繁时增加请求取消（AbortController），避免竞态与多次渲染；封装在 `fetchPrometheus` 层，参考 `src/components/PrometheusChart/utils/utils.js:48-68`。
- 采样补齐：在 `createDataHandler` 流程调用 `fillEmptyMetrics` 对 range 响应做时间点补齐，避免曲线断点与错位；参考 `src/resources/prometheus/monitoring.js:350-385` 与数据管线 `src/components/PrometheusChart/utils/index.js:32-61`。

### 稳健性与体验

- 错误展示细化：Prometheus 不可用或指标为空时，顶部卡与图卡展示占位与提示；现有错误捕获点在 `src/pages/compute/containers/Instance/Detail/Monitor/index.jsx:585-595`，完善文案。
- Loading 骨架：在 `VisibleObserver` 外层加骨架屏，避免首屏白板；参考 `src/components/PrometheusChart/BaseCard.jsx:135-144`。
- 文案与国际化：为新增“自创建至今”相关键补充 i18n 条目；涉及 `t('自创建')`、单位切换提示等。

## 验证

- 口径验证：新建实例“自创建 ≈ 今日”，历史实例“自创建>30 天”；比对后端扩展 API `origin_data.created/created_at`。
- 性能验证：连续刷新与区间切换时，请求数及响应稳定，无竞态错误；曲线在不同步长下平滑且不失真。
- 单位与统计验证：切换 B/s 和 bps 时刻度与数值一致、P95/平均/最大计算正确。

## 变更范围

- 仅前端代码变更，包含页面卡片配置、数据管线与小型工具函数扩展；不改动后端接口。

若你同意以上优化内容，我将按顺序逐项落地（先交互与统计、再查询与性能、最后稳健性与文案）。
